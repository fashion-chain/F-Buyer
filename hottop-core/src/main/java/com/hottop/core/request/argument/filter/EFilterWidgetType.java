package com.hottop.core.request.argument.filter;

public enum EFilterWidgetType {
    input, select, radio, checkbox, date,
    duration, durationDate,
    cascadeTree, image, imageArray,
    extra
}
